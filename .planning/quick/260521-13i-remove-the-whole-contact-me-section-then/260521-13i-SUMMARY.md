---
phase: 260521-13i
plan: "01"
type: quick
subsystem: home-page
tags: [contact-removal, cleanup, deploy]
key-files:
  modified:
    - src/app/[lang]/page.tsx
    - src/lib/useData.tsx
    - src/components/Home/Intro.tsx
  deleted:
    - src/components/Home/Contact/index.tsx
    - src/components/Home/Contact/SubmitButton.tsx
decisions:
  - Removed #contact CTA from Intro entirely (option a — minimally invasive) rather than repurposing it
  - Left server actions, reCAPTCHA provider, and i18n contact keys in place for reversibility
  - Removed BsLinkedin and Image unused imports from Intro as they were only referenced in commented-out blocks
metrics:
  completed: "2026-05-21"
  tasks: 2
  commits: 1
---

# Phase 260521-13i Plan 01: Remove Contact Section Summary

Removed the entire "Contact Me" form section from the Home page, cleaned all dangling nav/CTA references, and deployed to Vercel production. Build passes cleanly; the live site no longer renders a contact form.

## Tasks Completed

| Task | Name | Commit | Status |
|------|------|--------|--------|
| 1 | Remove Contact section and clean up dangling references | 9aeee20 | Done |
| 2 | Deploy to Vercel production | (deploy) | Done |

## Files Modified / Deleted

### Modified
- `src/app/[lang]/page.tsx` — removed `import Contact` and `<Contact t={t} />` JSX render
- `src/lib/useData.tsx` — removed Contact entry (`hash: "#contact"`, `key: 'Contact'`) from nav `links` array; removed `FaComment` from `react-icons/fa` import
- `src/components/Home/Intro.tsx` — removed `<Link href="#contact">` CTA block; removed now-unused imports: `Link` (next/link), `BsArrowRight`, `BsLinkedin` (react-icons/bs), `Image` (next/image), `useActiveSectionContext` and its destructured `setActiveSection`/`setTimeOfLastClick`

### Deleted
- `src/components/Home/Contact/index.tsx` — Contact form component (entire section)
- `src/components/Home/Contact/SubmitButton.tsx` — submit button used only inside Contact

### Left Untouched (as specified)
- `src/components/layout/Footer.tsx` — mailto link preserved
- `src/lib/consts.ts` — `contactEmail` still used by Footer and Intro email line
- `src/actions/sendEmail.ts` — server action, not wired to anything active
- `src/actions/verifyReCaptcha.ts` — server action, not wired to anything active
- `src/email/contact-form-email.tsx` — email template, not wired to anything active
- `src/app/GReCaptchaProvider.tsx` — provider, kept for atomic reversibility
- `src/app/i18n/dictionaries/*.js` — i18n keys (`contact`, `header.contact`, `intro.contact_me`, `toast.email_sent_success`) left in place

## Build Result

`npm run build` exits 0. No TypeScript or lint errors.

```
Route (app)                              Size     First Load JS
┌ ○ /_not-found                          871 B            88 kB
└ ƒ /[lang]                              25.4 kB         165 kB
```

## Vercel Production URL

https://brown-portfolio-bay.vercel.app

Deployment ID: `dpl_DDMAUoRuvkAc23ZP4XXnAEPP87Kd`
Inspect: https://vercel.com/hao-ping-chuangs-projects/brown-portfolio/DDMAUoRuvkAc23ZP4XXnAEPP87Kd

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Cleanup] Removed additional unused imports from Intro.tsx not mentioned in plan**
- **Found during:** Task 1
- **Issue:** After removing the #contact CTA, `BsLinkedin` (only in commented-out block) and `Image` (only in commented-out block) were also unused in active code.
- **Fix:** Removed both imports to keep the file clean and avoid potential lint warnings.
- **Files modified:** `src/components/Home/Intro.tsx`
- **Commit:** 9aeee20

## Deferred Cleanup (Optional, Out of Scope)

The following files are now orphaned but were intentionally left in place to keep this task atomic and reversible:

| File | Reason for deferral |
|------|---------------------|
| `src/actions/sendEmail.ts` | Only used by Contact form — safe to delete in a cleanup pass |
| `src/actions/verifyReCaptcha.ts` | Only used by Contact form — safe to delete in a cleanup pass |
| `src/email/contact-form-email.tsx` | Email template only used by sendEmail — safe to delete |
| `src/app/GReCaptchaProvider.tsx` | reCAPTCHA wrapper — check if used elsewhere before deleting |
| i18n keys (`contact`, `header.contact`, `intro.contact_me`, `toast.email_sent_success`) | Unused strings — no build impact; delete in a future i18n cleanup |

## Self-Check

- [x] `src/components/Home/Contact/` directory does not exist
- [x] No `from .*Home/Contact` imports in `src/`
- [x] No `href="#contact"` in `src/`
- [x] No `key: 'Contact'` in `src/`
- [x] Build exits 0
- [x] Vercel deploy exits 0 with production URL
