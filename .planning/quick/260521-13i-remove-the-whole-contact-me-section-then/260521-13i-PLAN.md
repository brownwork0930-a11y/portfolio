---
phase: 260521-13i-remove-the-whole-contact-me-section-then
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[lang]/page.tsx
  - src/components/Home/Contact/index.tsx
  - src/components/Home/Contact/SubmitButton.tsx
  - src/lib/useData.tsx
  - src/components/Home/Intro.tsx
autonomous: true
requirements:
  - QUICK-260521-13i
must_haves:
  truths:
    - "The Home page renders without the Contact form section"
    - "No nav link points to a missing #contact anchor"
    - "The Intro 'Contact Me' button no longer scrolls to a non-existent section"
    - "Build succeeds (no broken imports, no unused-import type errors)"
    - "Production deploy to Vercel completes successfully"
  artifacts:
    - path: src/app/[lang]/page.tsx
      provides: "Home page composition without Contact import or render"
    - path: src/lib/useData.tsx
      provides: "links array without Contact entry"
    - path: src/components/Home/Intro.tsx
      provides: "Intro without #contact CTA (or repurposed CTA)"
  key_links:
    - from: src/app/[lang]/page.tsx
      to: src/components/Home/Contact
      via: "import removed"
      pattern: "must NOT match: from .*Home/Contact"
    - from: src/lib/useData.tsx
      to: "links nav"
      via: "Contact entry removed"
      pattern: "must NOT match: hash: \\\"#contact\\\""
---

<objective>
Remove the entire "Contact Me" section from the portfolio Home page, clean up all references that would break the build or leave dangling navigation, then deploy to Vercel production.

Purpose: User wants the contact form section gone from the live site.
Output: A clean Home page without the Contact section, with a successful production deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Project: Next.js (App Router) portfolio site with i18n routes under `src/app/[lang]/`.

Relevant files discovered:
- `src/app/[lang]/page.tsx` — imports `Contact` from `@/components/Home/Contact` and renders `<Contact t={t} />` between `<Experience>` and `<BackToTop>`.
- `src/components/Home/Contact/index.tsx` — the Contact section component (form + reCAPTCHA + sendEmail).
- `src/components/Home/Contact/SubmitButton.tsx` — submit button used only inside Contact.
- `src/lib/useData.tsx` — defines the `links` nav array, which includes an entry `{ name: t.header.contact, hash: "#contact", key: 'Contact', icon: <FaComment /> }`. This drives the Header nav; leaving it in would point users to a dead anchor.
- `src/components/Home/Intro.tsx` — has a `<Link href="#contact">` "Contact Me" CTA that calls `setActiveSection("Contact")`. This would also dead-link after removal.
- `src/components/layout/Footer.tsx` — uses `contactEmail` from `@/lib/consts` for a `mailto:` link with text `{t.footer.contact_me}`. This is a direct mailto, NOT the Contact section, and should be LEFT ALONE (user said "remove the section", not all contact references).
- `src/lib/consts.ts` — `contactEmail` is also still used by Footer and by Intro's email line ("Email: ..."). Leave it.
- `src/actions/sendEmail.ts`, `src/actions/verifyReCaptcha.ts`, `src/email/contact-form-email.tsx`, `src/app/GReCaptchaProvider.tsx` — only used by the Contact form. Out of scope for this quick task: deleting them is optional cleanup but NOT required to ship. We leave them in place to keep this task atomic and reversible.

Deploy: `vercel --prod --yes` from project root (per task constraint).

<interfaces>
Current `src/app/[lang]/page.tsx` imports (relevant excerpt):
```tsx
import Contact from '@/components/Home/Contact';
// ...
<Experience t={t} data={experiencesData} />
<Contact t={t} />
<BackToTop t={t} />
```

Current nav link entry in `src/lib/useData.tsx`:
```tsx
{
  name: t.header.contact,
  hash: "#contact",
  key: 'Contact',
  icon: <FaComment />
},
```

Current Intro CTA in `src/components/Home/Intro.tsx` (around line 91-102):
```tsx
<Link
  href="#contact"
  className="..."
  onClick={() => {
    setActiveSection("Contact");
    setTimeOfLastClick(Date.now());
  }}
>
  {t.intro.contact_me}{" "}
  <BsArrowRight className="opacity-70 group-hover:translate-x-1 transition" />
</Link>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Contact section and clean up dangling references</name>
  <files>
    src/app/[lang]/page.tsx,
    src/lib/useData.tsx,
    src/components/Home/Intro.tsx,
    src/components/Home/Contact/index.tsx,
    src/components/Home/Contact/SubmitButton.tsx
  </files>
  <action>
    Make the following edits. Do NOT touch `src/components/layout/Footer.tsx` (its mailto link stays). Do NOT touch `src/lib/consts.ts` (`contactEmail` is still used elsewhere). Do NOT delete `src/actions/sendEmail.ts`, `src/actions/verifyReCaptcha.ts`, `src/email/contact-form-email.tsx`, or `src/app/GReCaptchaProvider.tsx` — leaving them in place keeps this task atomic and avoids cascading wiring removal.

    1) `src/app/[lang]/page.tsx`
       - Remove the line: `import Contact from '@/components/Home/Contact';`
       - Remove the JSX line: `<Contact t={t} />`
       - Leave the rest of the file untouched.

    2) `src/lib/useData.tsx`
       - Remove the Contact entry from the `links` array (the object containing `hash: "#contact"`, `key: 'Contact'`, and `name: t.header.contact`).
       - If `FaComment` is no longer referenced anywhere in the file, remove it from the `react-icons/fa` import to avoid unused-import lint warnings. (Other icons from that import like `FaBook`, `FaBriefcase`, `FaGraduationCap`, `FaProjectDiagram`, `FaUser` are still used and must stay.)

    3) `src/components/Home/Intro.tsx`
       - The Intro currently has a "Contact Me" CTA `<Link href="#contact" ...>`. Since `#contact` no longer exists, replace that CTA cleanly. Use option (a): remove the `<Link href="#contact">...</Link>` element entirely (keep the surrounding `motion.div` and the other button — the CV download — intact). This is the minimally-invasive option and matches the user's intent ("remove the whole contact me section").
       - Also remove the now-unused imports if they become unused as a result:
         - `Link` from `next/link` — KEEP only if still used elsewhere in the file; otherwise remove.
         - `BsArrowRight` from `react-icons/bs` — remove if no longer referenced. (`BsLinkedin` stays since the social icons row still uses it. If the file does not actually use `BsLinkedin`, leave imports as-is rather than guessing — verify by reading the file before editing.)
         - `useActiveSectionContext` and the destructured `setActiveSection`, `setTimeOfLastClick` — KEEP if used elsewhere in the file; remove only if the file no longer references them after the CTA is gone.
       - Be conservative: only remove imports/destructures that ripgrep confirms are no longer used in the file after the edit.

    4) `src/components/Home/Contact/index.tsx` and `src/components/Home/Contact/SubmitButton.tsx`
       - Delete both files (the entire `src/components/Home/Contact/` directory). They are no longer referenced after Step 1.
       - Use `rm` (or the Bash tool) to remove them so the directory is fully gone.

    Do NOT modify i18n dictionaries (`src/app/i18n/dictionaries/*.js`) — leaving the `contact`, `header.contact`, `footer.contact_me`, `intro.contact_me`, and `toast.email_sent_success` keys in place is fine; they are unused strings that do not affect the build and may be useful later. Removing them is out of scope for this quick task.
  </action>
  <verify>
    <automated>cd /Users/idea3c/conductor/workspaces/portfolio_brown/bucharest && npm run build 2>&1 | tail -50</automated>
    Additionally, run ripgrep to confirm there are no remaining live references to the Contact section:
    - `grep -rn "from .*Home/Contact" src/` should return no matches
    - `grep -rn "href=\"#contact\"" src/` should return no matches
    - `grep -rn "key: 'Contact'" src/` should return no matches
    - `ls src/components/Home/Contact 2>&1` should report the directory does not exist
  </verify>
  <done>
    - `npm run build` exits with code 0 (no TypeScript or lint errors related to missing imports / unused vars).
    - The Contact directory no longer exists on disk.
    - The Home page composition no longer imports or renders Contact.
    - The nav `links` array has no Contact entry.
    - The Intro no longer renders a `<Link href="#contact">` CTA.
    - Footer's mailto link is unchanged and still works.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel production</name>
  <files>(deploy only — no source files modified)</files>
  <action>
    From the project root `/Users/idea3c/conductor/workspaces/portfolio_brown/bucharest`, run:

    ```
    vercel --prod --yes
    ```

    Stream output. If Vercel CLI is not authenticated or the project is not linked, surface the error to the user and stop (do NOT attempt to authenticate interactively).

    On success, capture the production URL printed at the end of the Vercel output and include it in the task summary.
  </action>
  <verify>
    <automated>cd /Users/idea3c/conductor/workspaces/portfolio_brown/bucharest && vercel --prod --yes 2>&1 | tee /tmp/vercel-deploy-260521-13i.log; tail -20 /tmp/vercel-deploy-260521-13i.log</automated>
    The log must end with a line indicating successful deployment (typically a `https://...vercel.app` production URL and a "● Ready" or equivalent status). After deploy, optionally `curl -sI <prod-url>` should return HTTP 200.
  </verify>
  <done>
    - `vercel --prod --yes` exits 0.
    - A production URL is printed in the output.
    - Visiting the production URL shows the Home page without the Contact section (manual spot-check encouraged but not required for task completion).
  </done>
</task>

</tasks>

<verification>
End-to-end checks after both tasks complete:
1. `npm run build` succeeds locally.
2. `vercel --prod --yes` completes successfully and returns a production URL.
3. ripgrep confirms zero remaining live references to the Contact section in `src/` (see Task 1 verify).
4. Footer mailto link continues to work (unchanged).
</verification>

<success_criteria>
- Contact section component and directory removed.
- Home page renders cleanly without Contact, build passes.
- Nav and Intro no longer link to the dead `#contact` anchor.
- Production deploy to Vercel completes and returns a live URL.
</success_criteria>

<output>
After completion, create `.planning/quick/260521-13i-remove-the-whole-contact-me-section-then/260521-13i-SUMMARY.md` capturing:
- Files modified / deleted
- Build result
- Vercel production URL
- Any deferred cleanup (unused server actions, i18n keys, GReCaptchaProvider) for the user to consider later
</output>
